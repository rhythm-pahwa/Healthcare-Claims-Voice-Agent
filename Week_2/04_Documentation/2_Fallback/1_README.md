# Fallback

## Overview

The Fallback mechanism is used when the Healthcare Claims Voice Agent cannot confidently understand or process the user's request.

Instead of ending the conversation immediately, the agent attempts to recover the interaction by asking clarification questions or guiding the user towards supported services.

If the conversation cannot be recovered after the configured retry attempts, the request is transferred to the Human Escalation process.